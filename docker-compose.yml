services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: crq
      POSTGRES_PASSWORD: crq
      POSTGRES_DB: crq
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
