[project]
name = "crq-compliance"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.6","crq-core"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["crq_compliance*"]
