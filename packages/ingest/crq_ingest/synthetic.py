"""P1. See contracts/FUNCTIONS.md."""
from crq_core.schemas import OrgProfile, Snapshot


def generate_enterprise(profile: OrgProfile, asset_count: int, seed: int) -> Snapshot:
    """provenance MUST be SYNTHETIC."""
    raise NotImplementedError
